{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch App",
      "program": "${workspaceRoot}/server.js",
      "cwd": "${workspaceRoot}"
    }
  ]
}
